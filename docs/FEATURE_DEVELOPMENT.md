# 🚀 기능 개발 상세 명세서

## 🗓️ 전체 개발 순서

1. **[Phase 1] 상품/컨텐츠 시스템 (Items)** (우선순위 높음)

   - 사용자가 볼 수 있는 실제 컨텐츠 데이터를 구축합니다.
   - **Backend**: `Item` 모델 생성 (제목, 가격, 이미지, 설명, 카테고리)
   - **Frontend**: 메인 피드 API 연동, 상품 상세 페이지(`items/[id]`) 구현

2. **[Phase 2] 관심 상품 (Likes/Wishlist)**

   - 사용자의 취향을 저장하는 상호작용 기능입니다.
   - **Backend**: `Like` 모델 생성 (User - Item N:N 관계)
   - **Frontend**: 하트 버튼 토글 기능, '찜한 목록' 페이지 구현

3. **[Phase 3] 마이페이지 (Profile)**

   - 사용자 정보 관리 및 설정입니다.
   - **Frontend**: 프로필 정보 표시, 로그아웃, 내 활동 내역

4. **[Phase 4] 검색 (Search)**
   - 상품을 쉽게 찾을 수 있는 기능입니다.
   - **Backend**: 키워드 검색 API
   - **Frontend**: 검색 화면 UI 및 실시간 검색 결과

---

## 🛠️ 상세 기술 명세

### 1. 상품 시스템 (Items)

- **Backend**
  - Model: `Item`
    - `title`: string
    - `description`: text
    - `price`: integer
    - `image_url`: string
    - `category`: string (ex: 'food', 'toy', 'walk', 'care')
  - API: `GET /items` (목록), `GET /items/:id` (상세)
- **Frontend**
  - `src/app/page.tsx`: 더미 데이터 대신 API 데이터 바인딩
  - `src/app/items/[id]/page.tsx`: 상품 상세 화면 (이미지, 구매 버튼)

### 2. 좋아요 시스템 (Likes)

- **Backend**
  - Model: `Like`
    - `user_id`: integer (FK)
    - `item_id`: integer (FK)
  - API: `POST /items/:id/like` (토글), `GET /users/likes` (목록)
- **Frontend**
  - Component: `LikeButton` (하트 애니메이션)
  - Page: `src/app/likes/page.tsx` (찜한 목록 그리드)

### 3. 마이페이지 (My)

- **Frontend**
  - `src/app/profile/page.tsx`
  - 기능: 유저 이메일 표시, 로그아웃 버튼, 앱 버전 정보

### 4. 검색 (Search)

- **Frontend**
  - `src/app/search/page.tsx`
  - 기능: 추천 검색어, 키워드 입력 시 결과 리스팅
